<%= form_with(model: food_item, local: true) do |form| %>
  <% if food_item.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(food_item.errors.count, "error") %> prohibited this food_item from being saved:</h2>

      <ul>
      <% food_item.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= form.label :name %>
    <%= form.text_field :name, class:"form-control", id: :food_item_name  %>
  </div>

  <div class="form-group">
    <%= form.label :section_id %>
    <%= form.text_field :section_id, class:"form-control", id: :food_item_section_id %>
  </div>

  <div class="form-group">
    <%= form.label :description %>
    <%= form.text_area :description, class:"form-control", id: :food_item_description %>
  </div>

  <div class="form-group">
    <%= form.label :price %>
    <%= form.text_field :price, class:"form-control", id: :food_item_price %>
  </div>

  <div class="field">
    <%= form.label :remote_image_url %>
    <%= form.text_field :remote_image_url, class:"form-control", id: :food_item_remote_image_url %>
  </div>
    <br/>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
